This directory is kept for compatibility with the older per-demo SDK layout.

The project now resolves Meeting SDK headers from the shared repo-local `sdk/` directory
through CMake, so you do not need to copy SDK headers into this directory manually.
