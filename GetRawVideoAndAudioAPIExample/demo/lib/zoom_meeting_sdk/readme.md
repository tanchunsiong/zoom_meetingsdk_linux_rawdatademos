This directory is kept for compatibility with the older per-demo SDK layout.

The build now copies Meeting SDK runtime assets from the shared repo-local `sdk/`
directory into `demo/bin/`. You do not need to place `qt_libs`, `libmeetingsdk.so`,
`libcml.so`, or `libmpg123.so` here manually.
